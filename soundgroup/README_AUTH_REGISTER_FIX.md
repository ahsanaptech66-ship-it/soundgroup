# AUTH REGISTER RESPONSIVE FIX

Focused fix only for the public authentication modal.

- Keeps existing HTML/JS and registration logic unchanged.
- Constrains the auth card to the viewport.
- Makes the auth panel independently scrollable when registration fields exceed available height.
- Keeps the Create account button reachable on desktop and mobile.
- Reduces the artwork height on small screens so the form receives usable space.
- No database, API, player, playlist, Discover, Admin, or unrelated public feature changes.
